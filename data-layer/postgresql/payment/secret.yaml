---
# ExternalSecret: postgres-payment-admin
# Syncs the postgres superuser password from Vault KV.
# Reuses the same secret/data/postgres/payment path as the app-level ExternalSecret.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: postgres-payment-admin
  namespace: shopping-cart-data
  annotations:
    argocd.argoproj.io/sync-wave: "0"
  labels:
    app.kubernetes.io/name: external-secret
    app.kubernetes.io/instance: postgres-payment-admin
    app.kubernetes.io/component: database-credentials
    app.kubernetes.io/part-of: shopping-cart
    pci-scope: "true"
spec:
  refreshInterval: 24h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: postgres-payment-admin
    creationPolicy: Owner
  data:
    - secretKey: password
      remoteRef:
        key: secret/data/postgres/payment
        property: password
