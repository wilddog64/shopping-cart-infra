---
# Development secret - Replace with ExternalSecret in production
# See data-layer/secrets/postgres-payment-externalsecret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: postgres-payment-admin
  namespace: shopping-cart-data
  labels:
    app.kubernetes.io/name: postgresql
    app.kubernetes.io/instance: payment
    app.kubernetes.io/component: database
    app.kubernetes.io/part-of: shopping-cart
    pci-scope: "true"
  annotations:
    description: "Development secret - use ExternalSecret for production"
type: Opaque
stringData:
  password: "CHANGE_ME"
