---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: order-service
  namespace: cicd
  labels:
    app.kubernetes.io/name: order-service
    app.kubernetes.io/part-of: shopping-cart
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: shopping-cart

  source:
    # Update this to your actual Git repository URL
    repoURL: https://github.com/wilddog64/shopping-cart-order.git
    targetRevision: HEAD
    path: k8s/base

    kustomize:
      patches:
        - patch: |-
            - op: replace
              path: /spec/replicas
              value: 1
          target:
            kind: Deployment
        - patch: |-
            - op: add
              path: /spec/template/spec/imagePullSecrets
              value:
                - name: ghcr-pull-secret
          target:
            kind: Deployment

  destination:
    name: ubuntu-k3s
    namespace: shopping-cart-apps

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

  # Health checks
  ignoreDifferences:
    - group: apps
      kind: Deployment
      jsonPointers:
        - /spec/replicas  # Ignore HPA-managed replicas

  info:
    - name: Description
      value: Order Service - handles order creation and management
    - name: Team
      value: shopping-cart-team
    - name: Tier
      value: backend
